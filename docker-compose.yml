version: '3.8'

services:
  storm-multimedia:
    build: .
    image: storm-multimedia:latest
    container_name: storm-multimedia
    restart: always
    ports:
      - "3000:3000"
    volumes:
      - ./storm_multimedia.db:/app/storm_multimedia.db
      - ./public/avatars:/app/public/avatars
    environment:
      - PORT=3000
      - NODE_ENV=production
    healthcheck:
      test: ["CMD", "wget", "-qO-", "http://127.0.0.1:3000/api/profiles"]
      interval: 30s
      timeout: 5s
      retries: 3
      start_period: 10s
